class Snake (
    constructor( options = {} ) {
        this.options = {
            thickness: this.thickness,
            lenght: this.lenght,
            direction: this.direction,
            speed: this.speed || 4
        }
    }


)
